server:
  port: 8081

spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://localhost:5432/fake_payment_gateway
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format-sql: true

app:
